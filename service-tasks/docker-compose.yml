version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: meu-postgres
    environment:
      POSTGRES_DB: tasks
      POSTGRES_USER: root
      POSTGRES_PASSWORD: 1033
    ports:
      - "5432:5432"

